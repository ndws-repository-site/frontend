export interface StartTodayBackgroundImage {
    /** Отступ слева в % от левого края блока */
    left: string;
    /** Отступ сверху в % от верхнего края блока */
    top: string;
    /** Размер изображения в % от ширины блока */
    width: string;
    /** Поворот в градусах */
    rotate: number;
}
